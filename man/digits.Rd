% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digits.R
\name{digits}
\alias{digits}
\title{digits}
\usage{
digits(data, options, digits_1 = 2, digits_2 = 5)
}
\arguments{
\item{data}{A dataframe resulting from the 'step6' process of the `model_fit` function.}

\item{options}{all alternative options}

\item{digits_1}{The number of decimal places to retain for values related to the value function. 
The default is 2.}

\item{digits_2}{The number of decimal places to retain for values related to the action function. 
The default is 5.}
}
\value{
step7
}
\description{
digits
}
\examples{
data <- TAFC[TAFC$Subject == 1, ]

step1 <- unique_choice(
  data = data,
  L_choice = "L_choice", 
  R_choice = "R_choice"
 )
 
step2 <- arrange_data(
  data = step1[[1]],
  time_line = c("Block", "Trial")
)

step3 <- add_NA(
  data = step2
)

step4 <- set_initial_value(
  data = step3, 
  options = step1[["options"]], 
  initial_value = NA
)
step5 <- decision_making(
  data = step4,
  options = step1[["options"]],
  L_choice = "L_choice", R_choice = "R_choice",
  L_reward = "L_reward", R_reward = "R_reward",
  softmax = TRUE,
  threshold = 1,
  initial_value = NA,
  
  lambda = NA,
  gamma = 1,
  eta = c(0.3, 0.7),
  epsilon = NA,
  tau = 0.5
)

step6 <- model_fit(
  data = step5, 
  L_choice = "L_choice",
  R_choice = "R_choice",
  sub_choose = "Choose"
)

step7 <- digits(
  data = step6, 
  options = step1[["options"]],
  digits_1 = 2, 
  digits_2 = 5
)

}
